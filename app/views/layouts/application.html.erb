<!DOCTYPE html>
<html>
  <head>
    <title>Sns</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <header>
  <nav>
    <ul class="global-nav">
      <div class="logo">
        <li><%= link_to 'Deai',root_path %></li>
      </div>
     <div class="menu">
      <li><%= link_to "TopPage",root_path %></li>
       <% if user_signed_in? %>
        <li><%= link_to " Sigh Out ",destroy_user_session_path, method: :delete %></li>
       <% else %>
       <li><%= link_to "Log In",new_user_session_path %></li>
      <% end %>
      </div>
    </ul>
  </nav>
</header>
<div class="wrap">
<div class="era">
  <% flash.each do |name, msg| %>
  <%= content_tag(:div, msg, class: "alert") %>
 </div>
  <% end %>
<%= yield %>
</div>
  </body>
</html>
